# 2026-04-02

## 今日目标

- 给 Longform Library 补上真正可用的新增 / 删除闭环
- 重新审视 `AI analyse` 在长文详情里的位置，确认它是否符合用户心智
- 对当前分支的数据存储、文件存储和资源生命周期做一次严格健康审计
- 在收口实现后补齐日志与反思，并把分支推送、并回 `main`

## 今日产出

- Longform Library 新增与删除链路正式接通：
  - shelf 增加 `New Article`
  - detail 增加 `Delete`
  - 空库时也可直接从 0 到 1 新建第一篇
- Longform 删除已接到真实后端：
  - 新增 `DELETE /api/longform/:id`
  - 删除记录前会先清理对应封面文件
  - 删除后会返回已删条目，前端列表和当前选中态同步回退
- Longform 录入体验补强：
  - 封面除了上传外，也支持直接粘贴图片
  - 新建草稿不再依赖本地占位参考样本，真正以数据库内容作为事实源
- `AI analyse` 完成一轮第一性修正：
  - 从编辑工具组里移出，不再和 `Import text / Upload cover / Save` 混在一起
  - 改成右侧分析区的独立动作，语义从模糊的 AI 操作改成“拆解写法”
  - 分析只针对已保存文章，不再隐式先保存再分析
  - 增加分析状态提示：
    - `Not analyzed yet`
    - `Writing breakdown ready`
    - `Analysis outdated`
    - `Analysis failed`
  - 增加说明文案，明确它不是摘要，而是对文章为何成立、如何推进、哪些动作可复用的拆解
- 完成当前分支的数据层 / 文件层交叉审计：
  - API health 正常
  - migration 正常
  - 数据库引用与上传目录递归核对一致，没有孤儿文件，也没有丢失引用

## 结构 / 健康结论

- 当前仓库的数据边界仍然清晰：
  - 情绪板图片走 `entries.image_url`
  - Reactor 素材图走 `reactor_materials.meta.imageUrl`
  - Longform 封面走 `longform_entries.cover_image_path`
- 当前文件边界仍然可控：
  - 所有正式运行资源都统一落在 `ARTIFACTS/runtime/uploads`
  - 删除 entry、删除 reactor 图片素材、删除 longform 封面都已接上反向清理
  - 删除后会递归尝试回收空目录，没有出现“记录删了、文件还挂着”的明显腐化
- 当前没有发现数据库 / 文件系统失配：
  - `expected = 15`
  - `actual = 15`
  - `missingCount = 0`
  - `orphanCount = 0`
- 当前数据库计数如下：
  - `entries = 13`
  - `entry_terms = 102`
  - `week_notes = 15`
  - `day_notes = 1`
  - `reactor_materials = 8`
  - `longform_entries = 1`
- 仍然保留的轻债有两类：
  - 还没有“后台定期巡检孤儿文件”的自动化脚本
  - `lint` / `test` 仍未真正接线，当前健康主要靠 migration、typecheck、build 和人工验收

## 今日验证

- `GET /api/health` 返回正常：
  - `aiProvider = gemini`
  - `aiConfigured = true`
  - `uploadDir = /Users/guyongrui/.codex/worktrees/bbdb/handbook/ARTIFACTS/runtime/uploads`
- `bun run db:migrate` 通过
- `bun run typecheck:web` 通过
- `bun run typecheck:api` 通过
- `bun run build:web` 通过
- `bun run build:api` 通过
- 数据库与上传目录递归交叉检查结果：
  - `expected = 15`
  - `actual = 15`
  - `missingCount = 0`
  - `orphanCount = 0`

## Blockers / Wasted Motion

- 今日没有硬 blocker，主要时间消耗在：
  - 先看错了分支，一度基于 `main` 做了错误判断，后面切回 `reactor-weekly-board` 才对齐真实进度
  - Longform 的新增 / 删除能力后端其实已有一半改动在进行中，补的时候需要先辨别哪些是“已开始但未收口”的改动，避免互相覆盖
- 一个值得记住的 wasted motion 是：
  - 如果不先用第一性原理问清楚“用户来这里的第一任务是什么”，很容易把一个高阶能力错误地摆到主流程按钮里，制造伪顺滑

## 一个该改变的默认值

- 以后只要一个 AI 按钮不是在做“主任务”，就默认先问它是不是应该退到二级区域，而不是和保存、导入、上传并列

## PM 风险

- Longform 现在已经具备录入、保存、删除、拆解的最小闭环，但“拆解结果如何被继续复用到写作动作里”还没有真正接上；如果停在展示层，它会重新变成一个好看的分析侧栏，而不是创作工具

## 明日下一步

- 继续把 Longform 的拆解结果接回创作闭环：
  - 看是否需要“复制拆解动作”
  - 看是否需要“用这篇文章生成一份写作 prompt”
  - 看是否需要把拆解过期状态做得更明显
- 把今天完成的分支推送、并回 `main`，确保这轮收口不只停在本地工作区
