# 2026-04-01

## 今日目标

- 把 Reactor 当前这一轮交互打磨正式收口
- 更新日志与反思，避免连续数轮迭代只留在代码里、不沉淀判断
- 对项目目录、持久化、本地存储、数据库和上传目录做一次系统健康检查
- 在进入下一阶段关系型交互前，确认当前基座没有明显腐化

## 今日产出

- Reactor 卡片动作继续收口：
  - `Copy Link` 增加了轻反馈
  - `Edit / Delete` 进入稳定可见状态，并收进卡片边界内，避免被裁切
  - 编辑浮层支持直接查看元数据，而不只是改备注
- Reactor 新建与编辑规则更清晰：
  - 新卡片默认自动带上类型 tag
  - `Why keep it` 提供常用短句预设，减少重复输入
  - `Link` 可直接查看并跳转原始链接
  - `Image` 可直接预览并打开大图
- Link preview 链路加固：
  - metadata 解析不再依赖固定属性顺序
  - 请求头改为更接近真实浏览器
  - 能抓到的外链封面会优先缓存到本地 `/uploads`
  - 删除卡片时，本地缓存资源能一起被清理
- 本地存储加固：
  - 读取布局和尺寸时统一走安全 JSON 解析
  - 已删除卡片的本地布局残留会自动清掉
- 项目健康检查完成：
  - 数据库 migration 状态正常
  - API 健康检查正常
  - 上传目录已与数据库引用对齐
  - 孤儿上传文件已清理

## 结构 / 健康结论

- 当前目录结构仍然清晰，`apps / packages / SPECS / TASKS / WORKLOG / .learnings / ARTIFACTS` 边界正常，没有出现明显“想到哪放到哪”的腐化迹象
- `dist` 和 `node_modules` 都被 `.gitignore` 正确忽略，没有把构建产物和依赖目录污染进版本控制
- 数据模型目前仍是可控的：
  - 情绪板图片流和 Reactor 素材流已分开建模
  - `reactor_materials.meta` 虽然是 `jsonb`，但现在承载的字段仍然有限，没有演变成无边界垃圾桶
- 风险点仍然存在两个：
  - 根脚本里还没有真正接上 `lint` / `test`，工程健康目前主要靠 typecheck、build 和人工验收
  - Reactor 的关系型交互还没开始，所以“数据可整理性”还没被真实高压验证

## 今日验证

- `bun run db:migrate` 通过
- `bun run typecheck:api` 通过
- `bun run typecheck:web` 通过
- `cd apps/api && bun run build` 通过
- `bun run build:web` 通过
- `GET /api/health` 返回正常：
  - `aiProvider = gemini`
  - `aiConfigured = true`
  - `uploadDir` 指向 repo root 下的 `ARTIFACTS/runtime/uploads`
- 数据库与文件系统交叉检查结果：
  - `weeks = 15`
  - `entries = 10`
  - `reactor_materials = 11`
  - `failed_entries = 0`
  - 上传目录清理后：
    - `orphanCount = 0`
    - `missingCount = 0`

## Blockers / Wasted Motion

- 今日没有硬 blocker，主要是一些健康检查的机械性收口工作
- wasted motion 主要在：
  - link preview 为了更稳，前后做了几轮 fetch / metadata / 图片缓存策略修补
  - shell 和临时脚本路径有一点时间消耗，但没有影响最终判断

## 一个该改变的默认值

- 以后只要一条新能力涉及“数据库记录 + 本地文件 + 本地存储 + UI 状态”四层中的两层以上，就默认在当日收口时做一次交叉健康检查，不要等出问题再补

## PM 风险

- 当前 Reactor 的基础质感和捕获能力已经够强，如果下一步还继续做表层 polish，而不是尽快进入 `靠近 / 成组 / 子集 / merge`，产品会开始出现“好看但不更聪明”的停滞感

## 明日下一步

- 进入 Reactor 单日画布的第一批关系型交互：
  - 靠近提示
  - 成组
  - 子集挂靠
  - 删除前归并判断
