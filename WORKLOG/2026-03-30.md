# 2026-03-30

## 今日目标

- 收口目录结构，避免空壳目录继续制造歧义
- 确认删除图片时会同步删除磁盘文件
- 让上传图片自带日期路径，便于后续排查与管理
- 补齐 README 的启动与存储说明

## 今日产出

- 清理顶层空占位目录，删除 `src/.gitkeep` 与 `tests/.gitkeep`
- 确认仓库顶层只保留真正参与运行与协作的目录
- 上传图片改为按日期落盘到 `ARTIFACTS/runtime/uploads/YYYY/MM/DD/`
- 新增磁盘文件删除能力，entry 删除时会同步清理图片文件
- 删除图片后会尝试回收空的日期目录，避免上传目录越积越散
- README 补充“按日期存储 + 删除即删文件”的说明
- 复核本地启动命令与 workspace 脚本，确认 README 与实际脚本一致

## 关键决定

- 目录整理优先减歧义，不保留“也许以后会用”的空壳目录
- 图片文件命名要能直接暴露时间信息，方便 Agent 和人一起排查问题
- 删除 entry 的语义必须包含“删除对应文件”，否则存储会不可控膨胀
- README 继续作为项目入口文档，优先保证启动、存储、边界信息低歧义

## 今日验证

- `git status --short` 确认当前变更仅聚焦在目录整理、图片存储和 README
- `find . -maxdepth 2 -type d | sort` 确认顶层已无空 `src/`、`tests/`
- `bun run typecheck:api` 通过
- `bun run build:api` 通过
- `bun run typecheck:web` 通过
- `bun run build:web` 通过
- 真实创建一条 entry，图片路径形如：
  - `/uploads/2026/03/30/20260330-094725-...png`
- 真实删除该 entry 后，磁盘文件已消失
- API 与磁盘文件删除行为已对齐，不再只删数据库记录

## 风险 / 轻债

- 旧历史图片记录虽然已可正常删除，但还缺少“后台清理孤儿文件”的定期巡检脚本
- `lint` 与 `test` 仍未正式接线，当前仍以类型检查和运行时回归兜底

## 下一步

- 给上传与删除主链路补一条最小自动化回归
- 继续收口 UI 细节，但不再让界面状态先于数据边界
